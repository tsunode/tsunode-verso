import { Header } from '../../components/Header';

import { Section } from './styles';

export const NewProject = () => {
  console.log('te');

  return (
    <main>
      <Header />

      <Section />
    </main>
  );
};
